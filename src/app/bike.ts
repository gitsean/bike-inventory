export class Bike {
  constructor(
    public brand: string,
    public model: string,
    public year: string,
    public size: string,
    public imgUrl: string,
    public price: string
  ) {  }
}

// A bike has a brand, a model, year, a color, and some other things perhaps.